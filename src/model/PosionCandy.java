package model;

import controller.Game;
import utils.ObjectId;

public class PosionCandy extends BasicCandy {

	public PosionCandy(int x, int y, ObjectId id, int candyScore, Game game) {
		super(x, y, id, candyScore, game);
	}
	
}
